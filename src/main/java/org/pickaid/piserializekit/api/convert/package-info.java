/**
 * KubeJS-neutral value conversion helpers for typed ids, script-facing inputs, and
 * compatibility packs that need stable diagnostics without depending on one script runtime.
 */
package org.pickaid.piserializekit.api.convert;
