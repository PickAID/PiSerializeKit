/**
 * Stable packet-buffer adapters for PiSerializeKit packet codecs.
 *
 * <p>Version-sensitive Minecraft buffer calls should be localized behind this
 * package so codec logic and generated bindings can stay stable across upgrades.</p>
 */
package org.pickaid.piserializekit.api.packet.buffer;
