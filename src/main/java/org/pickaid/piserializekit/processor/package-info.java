/**
 * Annotation processor internals that turn authored Pi schema and packet types
 * into generated runtime bindings.
 *
 * <p>This package is intentionally internal. Public authoring happens through
 * annotations and base types in the {@code api} packages.</p>
 */
package org.pickaid.piserializekit.processor;
